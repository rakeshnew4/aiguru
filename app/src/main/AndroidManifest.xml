<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
        Required on Android 11+ (API 30+) so the Razorpay SDK can query
        UPI apps (PhonePe, GPay, Paytm, BHIM, etc.) and launch them via
        an explicit Intent rather than getting a "no matching activity" error.
    -->
    <queries>
        <!-- PhonePe -->
        <package android:name="com.phonepe.app" />
        <!-- Google Pay -->
        <package android:name="com.google.android.apps.nbu.paisa.user" />
        <!-- Paytm -->
        <package android:name="net.one97.paytm" />
        <!-- BHIM -->
        <package android:name="in.org.npci.upiapp" />
        <!-- Amazon Pay -->
        <package android:name="in.amazon.mShop.android.shopping" />
        <!-- WhatsApp Pay / Mi Pay / Samsung Pay etc. catch-all via UPI scheme -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="upi" />
        </intent>
    </queries>

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="AI Guru"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AIGuru"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <!--
            SplashActivity is the true entry-point.
            It performs an update/maintenance check before routing to HomeActivity.
        -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AIGuru.Splash"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- HomeActivity is internal — reached only via SplashActivity. -->
        <activity
            android:name=".HomeActivity"
            android:exported="false" />

        <activity android:name=".LoginActivity" android:exported="false"/>
        <activity android:name=".EmailAuthActivity" android:exported="false" android:windowSoftInputMode="adjustResize"/>
        <activity android:name=".SchoolLoginActivity" android:exported="false"/>
        <activity android:name=".SignupActivity" android:exported="false" />
        <activity android:name=".UserProfileActivity" android:exported="false" />
        <!-- exported + singleTop required for Razorpay UPI redirect (PhonePe etc.)
             to return control to this activity after payment completes. -->
        <activity android:name=".SubscriptionActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Razorpay uses this scheme to redirect back after UPI payment -->
                <data android:scheme="razorpay" />
            </intent-filter>
        </activity>
        <activity android:name=".SubjectActivity" android:exported="false"/>
        <activity android:name=".ChapterActivity" android:exported="false"/>
        <activity android:name=".ChatActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"/>
        <activity android:name=".RevisionActivity" android:exported="false"/>
        <activity android:name=".ModelSettingsActivity" android:exported="false"/>
        <activity android:name=".LibraryActivity" android:exported="false"/>
        <activity android:name=".PageViewerActivity" android:exported="false"/>
        <activity android:name=".FullscreenImageActivity" android:exported="false"
            android:theme="@style/Theme.AIGuru.Fullscreen"/>
        <activity android:name=".ProgressDashboardActivity" android:exported="false"/>
        <!-- Quiz system -->
        <activity android:name=".QuizSetupActivity" android:exported="false"/>
        <activity android:name=".QuizActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"/>
        <activity android:name=".QuizResultActivity" android:exported="false"/>
        <!-- Blackboard step-by-step audio-visual lesson mode -->
        <!-- singleTop: prevents multiple BB instances stacking up when back is pressed -->
        <activity android:name=".BlackboardActivity" android:exported="false"
            android:screenOrientation="portrait"
            android:launchMode="singleTop"/>
        <activity android:name=".GeminiLiveActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"/>

        <!-- UCrop image cropper activity -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>